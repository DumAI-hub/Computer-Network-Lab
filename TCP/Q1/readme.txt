Write a client/server program in C using TCP socket to do the following:
    The client will send a string  to the server
    The server will send the string back to the client

To find the inet address ->
    1. open your terminal
    2. type ifconfig(for linux based systems)/ipconfig(for windows)
